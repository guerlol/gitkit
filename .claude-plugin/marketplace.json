{
  "$schema": "https://anthropic.com/claude-code/marketplace.schema.json",
  "name": "gitkit",
  "description": "gitkit — everyday git workflow commands and a staged-secret guard for Claude Code",
  "owner": {
    "name": "guerlol",
    "url": "https://github.com/guerlol"
  },
  "plugins": [
    {
      "name": "gitkit",
      "description": "Everyday git workflow: commit messages that match the repo, branch naming, PRs, sync, undo — plus a staged-secret guard that blocks commits containing keys or tokens",
      "source": "./",
      "category": "productivity",
      "keywords": ["git", "commit", "pull-request", "secrets", "pre-commit"]
    }
  ]
}
